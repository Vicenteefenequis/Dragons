import React from 'react';
import { DefaultLayout } from '../../components';

const Home = (): JSX.Element => {
  return (
    <DefaultLayout>
      <p>ola home</p>
    </DefaultLayout>
  );
};

export default Home;
